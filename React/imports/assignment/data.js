const company = "QSpiders";
const location = "Bangalore";

function displayInfo() {
  console.log(`Company name is ${company} and location is ${location}`);
}

export default { company, location, displayInfo };
